const srcPath = 'src';

const config = {
    src: {
        root: srcPath,
        html: srcPath,
        css: `${srcPath}/css`,
        sass: `${srcPath}/scss`,
        fonts: `${srcPath}/fonts`,
        images: `${srcPath}/images`
    }
};

export default config;